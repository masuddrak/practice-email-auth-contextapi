

const InputForword = ({type,placeholder}) => {
    return (
        <input className="bg-gray-400 text-black" type={type} placeholder="ugujki"/>
    );
};
// const InputForwordRef= React.forwardRef(InputForword)
export default InputForword;